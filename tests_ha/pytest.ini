# A separate config so `pytest tests_ha` finds this one first and runs real Home
# Assistant in auto asyncio mode, while a bare `pytest` at the root still gets the
# stub suite in tests/ and needs nothing installed but pytest.
[pytest]
asyncio_mode = auto
testpaths = .
